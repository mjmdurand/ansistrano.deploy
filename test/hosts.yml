---
all:
  vars:
    ansible_host_key_checking: false
    ansible_user: ansistrano
    ansible_ssh_pass: ansistrano
    ansible_become_password: ansistrano
  hosts:
    amazonlinux:
      ansible_host: amazonlinux

    debian:
      ansible_host: debian

    fedora:
      ansible_host: fedora

    opensuse:
      ansible_host: opensuse

    rockylinux:
      ansible_host: rockylinux

    ubuntu:
      ansible_host: ubuntu