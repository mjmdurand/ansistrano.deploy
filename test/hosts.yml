---
all:
  vars:
    ansible_host_key_checking: false
    ansible_user: root
    ansible_ssh_private_key_file: ./deploy.key
  hosts:
    amazonlinux:
      ansible_host: amazonlinux

    debian:
      ansible_host: debian

    fedora:
      ansible_host: fedora

    opensuse:
      ansible_host: opensuse

    rockylinux:
      ansible_host: rockylinux

    ubuntu:
      ansible_host: ubuntu